package com.gmail.realtadukoo.util.event.view;

import com.gmail.realtadukoo.util.event.EventHandler;

/**
 * {@link EventHandler} for creating {@link ViewChangeEvent ViewChangeEvents} 
 * and sending them to the registered {@link ViewChangeEventListener ViewChangeEventListeners}.
 * 
 * @author Logan Ferree (Tadukoo)
 * @version 0.1-Alpha-SNAPSHOT
 */
public abstract class ViewChangeEventHandler extends EventHandler<ViewChangeEvent, ViewChangeEventListener>{
	
}
